# 자바스크립트 (Javascript)
깊은 복사(클론(clone)을 만드는 것): 복사한 두 배열이 완전히 동립적으로 독사
얕은 복사(참조 복사): 같은 배열에 이름만 하나 더 붙는 것
문서 객체(document object): HTML에 태그 요소들(html, head, body, title, h1, dev ...)
요소(element): HTML에 태그요소들 자바스크립트의 문서 객체와 동일한 뜻이다.
문서 객체 모델 DOM(Document Object Models): 문서 객체를 조합해서 만든 전형적인 형태 
CSS 선택자{ // "이름" "선택자 형태" "설명"
  태그 선택자,  태그,  특정 태그를 가진 요소를 추출합니다.
  아이디 선택자, #아이디, 특정 Id 속성을 가진 요소를 추출합니다.
  클래스 선택자, .클래스, 특정 class 속성을 가진 요소를 추출합니다.
  속석 선택자, [속성 = 값], 특정 속성 값을 갖고 있는 요소를 추출합니다.
  후손 선택자, 선택자_A 선택자_B, 선택자_A 아래에 있는 선택자_B를 선택합니다.
}
DOMContentLoaded: 이벤트는 HTML페이지의 모든 문서 객체(요소)를 웹 브라우저가 읽어들였을 때  발생시키는 이벤트
querySelector(): 이 메소드는 문서 객체를 선택할 때 사용하는 메소드
textContent속성과 innerHTML속성: 문서 객체의 내부의 글자를 조작할 때 사용하는 속성
style: 이 속성은 문서 객체의 스타일을 조작할 때 사용하는 속성
이벤트 모델(Event Model): 이벤트를 연결하는 방식
표준 이벤트 모델 [
  document.body.addEventListener('keyup', () => {
  
  })
]

고전 이벤트 모델 [
  document.body.onkeyup = (event) => {

  }
]

인라인 이벤트 모델 [
  <script>
    const listener = (event) => {

    }
  </script>
  <body onkeyup="listener(event)">

  </body>
]

조기 리턴(early return): 예상된 결과가 아닐 경우 미리 리턴하여 쓸모 없는 뒤의 코드를 실행시키지 않는 것
드랍다운 목록(drop down): [
  기본: select태그
  <select>
    <options> </options>
    ...
  </select>
]
인풋 박스(input box): [
  기본: input
    <input> </input>
]
